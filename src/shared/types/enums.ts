export enum ApiMethod {
  Get = 'GET',
  Post = 'POST',
  Put = 'PUT',
  Patch = 'PATCH',
  Delete = 'DELETE',
}

export enum SortOrder {
  Asc = 'asc',
  Desc = 'desc',
}

export enum AccentColors {
  Primary = 'Primary',
  Secondary = 'Secondary',
  Tertiary = 'Tertiary',
  Error = 'Error',
}

export enum FontSize {
  Mini = '1.2rem',
  SM = '1.4rem',
  MD = '1.6rem',
  LG = '2rem',
  XLG = '3.2rem',
}

export enum Variant {
  P = 'P',
  H1 = 'H1',
  H2 = 'H2',
  Span = 'Span',
}
